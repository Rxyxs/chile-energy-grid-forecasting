# Datos, features y modelado
pandas
numpy
polars
holidays
lightgbm
xgboost
scikit-learn
optuna
joblib

# 3er enfoque de modelado: MLP en PyTorch con loss custom (Huber) y
# comparación de activaciones ReLU/GELU/Swish (src/models/torch_forecaster.py).
# CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
duckdb

# Dashboard
streamlit
plotly

# Notebook (02_Weather_Augmented_Rolling_CV.ipynb)
jupyter
nbformat
nbconvert
matplotlib

# Tests
pytest

# NOTA: prophet / neuralprophet / sktime se agregarán cuando se implemente esa
# funcionalidad específica. prophet en particular requiere un toolchain de
# compilación (cmdstanpy) que conviene instalar y validar por separado.
